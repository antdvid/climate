#!/bin/bash
#PBS -l nodes=1:ppn=32
#PBS -l walltime=24:00:00
#PBS -m abe 
#PBS -l mem=1000mb
#PBS -j oe

DIR="/home/zgao/FronTier++_climate/climate"
echo $DIR

#directory of excutable

#run options
echo input file $DIR/in-$prob
echo output directory $DIR/out-$prob
mpirun -np 16 --bind-to-core --bysocket $DIR/climate -d 2 -p 4 4 -i $DIR/in-$prob -o $DIR/out-$prob
